{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window. Deliberately minimal: the frontend reaches the filesystem only through Tauri commands that go through path containment, so the fs plugin is granted nothing.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "core:webview:allow-create-webview-window",
    "core:webview:allow-webview-close",
    "core:window:allow-set-focus",
    "opener:default",
    "dialog:default",
    "core:window:allow-unminimize",
    "core:window:allow-minimize"
  ]
}
